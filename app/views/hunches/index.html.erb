<h1>Listing hunches</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Geographicarea</th>
    <th>Sector</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @hunches.each do |hunch| %>
  <tr>
    <td><%= hunch.title %></td>
    <td><%= hunch.description %></td>
    <td><%= hunch.Geographicarea %></td>
    <td><%= hunch.Sector %></td>
    <td><%= link_to 'Show', hunch %></td>
    <td><%= link_to 'Edit', edit_hunch_path(hunch) %></td>
    <td><%= link_to 'Destroy', hunch, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Create a Hunch', new_hunch_path %>
