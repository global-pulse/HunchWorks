<%= form_for(@hunch) do |f| %>
  <% if @hunch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hunch.errors.count, "error") %> prohibited this hunch from being saved:</h2>

      <ul>
      <% @hunch.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :"Geographical area" %><br />
    <%= f.text_field :Geographicarea %>
  </div>
  <div class="field">
    <%= f.label :Sector %><br />
    <%= f.text_field :Sector %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
